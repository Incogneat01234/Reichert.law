---
layout: base.njk
title: Blog | Thomas Reichert
description: Doctrine and Data - Commentary on intellectual property law, empirical legal studies, and trademark doctrine.
permalink: /blog/index.html
---

<main>
    <section class="consulting-hero">
        <div class="consulting-hero-container">
            <h1>Doctrine & Data</h1>
            <p class="tagline">Commentary & Analysis</p>
            <p>Thoughts on intellectual property law, empirical legal studies, trademark doctrine, and the intersection of law and technology.</p>
            <a href="/blog/feed.xml" class="rss-link" title="Subscribe via RSS">
                <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="currentColor"><path d="M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1Z"/></svg>
                RSS Feed
            </a>
        </div>
    </section>

    <section class="blog-section">
        <div class="blog-container">
            {% if collections.posts.length > 0 %}
                {% if collections.tagList.length > 0 %}
                <div class="blog-controls">
                    <div class="blog-tags">
                        <span class="filter-label">Tags:</span>
                        {% for tag in collections.tagList %}
                        <a href="/blog/tags/{{ tag | slugify }}/" class="tag-filter">{{ tag }}</a>
                        {% endfor %}
                    </div>
                </div>
                {% endif %}

                <div class="blog-posts">
                    {% for post in collections.posts %}
                    <article class="post-card">
                        <a href="{{ post.url }}" class="post-card-link">
                            <div class="post-card-content">
                                <div class="post-meta-row">
                                    <time class="post-date">{{ post.date | dateDisplay }}</time>
                                    {% if post.data.category %}
                                    <span class="post-category">{{ post.data.category }}</span>
                                    {% endif %}
                                </div>
                                <h2 class="post-title">{{ post.data.title }}</h2>
                                {% if post.data.series %}
                                <div class="post-series-info">Part {{ post.data.series.part }} of <em>{{ post.data.series.name }}</em></div>
                                {% endif %}
                                <p class="post-excerpt">{{ post.data.excerpt }}</p>
                                {% if post.data.tags %}
                                <div class="post-tags">
                                    {% for tag in post.data.tags %}{% if tag != "posts" %}
                                    <span class="post-tag">{{ tag }}</span>
                                    {% endif %}{% endfor %}
                                </div>
                                {% endif %}
                            </div>
                        </a>
                    </article>
                    {% endfor %}
                </div>
            {% else %}
                <div class="blog-no-posts">
                    <p>New posts coming soon. In the meantime, check out the <a href="https://doctrineanddata.blogspot.com/" target="_blank" rel="noopener">archive on Blogspot</a>.</p>
                </div>
            {% endif %}
        </div>
    </section>
</main>
