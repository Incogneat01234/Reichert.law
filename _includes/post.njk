<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | Doctrine & Data</title>
    <meta name="description" content="{{ excerpt }}">
    <meta property="og:title" content="{{ title }} | Doctrine & Data">
    <meta property="og:description" content="{{ excerpt }}">
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{ site.url }}{{ page.url }}">
    <meta property="og:image" content="{{ site.url }}/og-image.jpg">
    <meta name="twitter:card" content="summary_large_image">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <link rel="alternate" type="application/rss+xml" title="Doctrine & Data RSS Feed" href="/blog/feed.xml">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=Source+Sans+3:ital,wght@0,300;0,400;0,500;0,600;1,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <link rel="canonical" href="{{ site.url }}{{ page.url }}">
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "BlogPosting",
        "headline": "{{ title }}",
        "description": "{{ excerpt }}",
        "author": {
            "@type": "Person",
            "name": "Thomas Reichert",
            "url": "https://reichert.law"
        },
        "datePublished": "{{ date | dateToRfc3339 }}",
        "url": "{{ site.url }}{{ page.url }}"
    }
    </script>
</head>
<body>
    {% include "header.njk" %}

    <main>
        <article class="blog-post">
            <header class="blog-post-header">
                <div class="blog-post-meta">
                    <time class="blog-post-date">{{ date | dateDisplay }}</time>
                    {% if category %}
                    <span class="post-category">{{ category }}</span>
                    {% endif %}
                </div>
                <h1 class="blog-post-title">{{ title }}</h1>
                {% if tags %}
                <div class="blog-post-tags">
                    {% for tag in tags %}{% if tag != "posts" %}
                    <a href="/blog/tags/{{ tag | slugify }}/" class="post-tag">{{ tag }}</a>
                    {% endif %}{% endfor %}
                </div>
                {% endif %}
            </header>

            <div class="blog-post-content">
                {{ content | safe }}
            </div>

            <footer class="blog-post-footer">
                <a href="/blog/index.html" class="blog-back-link">← Back to all posts</a>
            </footer>
        </article>
    </main>

    {% include "footer.njk" %}
</body>
</html>
